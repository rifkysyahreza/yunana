Description: i want to create a token monitor bot to help me doing DLMM trade in meteora, which the bot will be used as an early token spotter & pool scanner.

Flow Summary: 1. Monitor solana chain using Helius API every 15s, don't scan backward to the past, but monitor for freshly bonded pumpfun token, (Scan: Present → Future). 2. After bot get token CA (Contract Address), Scan it using GMGN API, the total_fee and market cap ratio should be 1 SOL per 10k market cap. 3. If the token didn't meet the total_fee and market cap ratio, just discard it. If meet the ratio, then scan pool using Meteora API. 4. Send the token details to telegram.

Messages Format:

```
{Token Banner}

<u>Token Details</u>
CA: EPuZ1X6pPzac3ELPsT59LStmgaSr4kBJvaAbL15Fpump (monospace format in telegram, so it can be copied easily)
Token Name: Distorted Face
Token Symbol: Distorted

<u>Token Stat</u>
Total fee: 1
Market cap: 10k

<u>Pool Details</u>
Pool Address: 6MvXbwUdrbp1hifHABfDAbNrhvvX1yHnaTpdArjj1VYB (or "None" if not found)

<u>Quick Action</u>
GMGN: Link (Hyperlink)
Meteora: Link (Hyperlink, or "None" if not found)

```
